# CLAUDE.md

Guidance for Claude Code when working in this repo.

## Project

Multi-step lead-intake web form for Ignite Individual Coaching. Static frontend (vanilla JS/HTML/CSS) + one Netlify serverless function. See `README.md` for architecture, form flow, and repository structure — keep that as the source of truth and update it when structure changes.

## Conventions

- No frontend framework — vanilla JS only.
- Form is schema-driven (`js/formSchema.js`); steps/branching are data, not markup.
- Field IDs are stable across `formSchema.js`, locale files, payload, and spreadsheet columns — only labels change per locale.
- Locale strings live in `/locales/<locale>.json`, keyed by translation ID (e.g. `form.step1.nome`), not by page.
- `.env` holds secrets, never commit it — `.env.example` documents the required keys.

## Progress log

- **Issue #1 — Project scaffold and tooling**: created base folder structure (`css/`, `js/`, `locales/`, `netlify/functions/`), stub `index.html`, `.env.example`, `netlify.toml`, `package.json` (netlify-cli dev dependency), added `.netlify/` to `.gitignore`, and a "Getting started" section in the README.
  - Known issue: `npm install` may time out downloading `netlify-cli`'s `sharp` dependency (native binary fetch) depending on network — not a scaffold defect, retry or install on a better connection.
- **Issue #2 — i18n infrastructure**: implemented `loadLocale(locale)` and `t(key)` in `js/i18n.js` (fetches `locales/<locale>.json`, missing keys log a `console.warn` and fall back to the key itself instead of throwing). Seeded `locales/pt-PT.json` with a placeholder `app.title` key and wired `js/formEngine.js` to load the locale and render through `t()` instead of a hardcoded string.
- **Issue #3 — Form schema and data model**: filled `js/formSchema.js` with the 5 steps (`dados_basicos`, `treino_geral`, `fase_gestacao`, `gestacao`, `posparto`) as data-only objects — fields carry `id`/`type`/`required`/`labelKey`/`options`, steps and some fields carry pure `condition(answers)` predicates. Helpers: `selectedGestacaoPosparto`, `visibleSteps`, `visibleFields`. `answers` is a flat object keyed by field id (radio → option id, multi checkbox → array). Field-ID reference in `docs/field-ids.md`. Branch logic covered by `test/formSchema.test.mjs` (`npm test` → `node --test`). Locale keys for every `labelKey` seeded in `locales/pt-PT.json`.
- **Issue #4 — Form engine: step rendering and navigation**: `js/formEngine.js` now reads the schema and drives the form. Exports a mutable `state` (`{ answers, currentStepId }` — flat, keyed by field id), pure DOM-free nav helpers (`nextVisibleStep`/`prevVisibleStep`/`currentStepIndex`, all derived from `visibleSteps(answers)` — no history stack, so changing an earlier answer transparently reroutes the remaining steps), `renderField`/`renderStep`, `goNext`/`goBack`, and `init()`. Rendering by field `type`: text/email/tel → `input`; radio → `fieldset` of radios; multi-`checkbox` (has `options`) → `fieldset`, answer is `string[]`; bare `checkbox` (no `options`, e.g. `aviso_contacto`) → single box, answer is boolean. Text inputs update `state` on `input` without re-render (keeps focus); radio/checkbox changes re-render so field/branch `condition`s re-evaluate live. Nav buttons are `type="button"` — no reload; `index.html` calls `init()` from an inline module. Validation and submit stay out of scope. Tests: `test/formEngine.test.mjs` (jsdom `devDependency`; a `fetch` shim reads the locale JSON so nothing test-only ships in `i18n.js`).
- **Issue #5 — Field validation**: `js/formEngine.js` blocks "next" while a visible `required` field is empty. Pure helpers `isFieldFilled(field, answers)` (empty-check per type: trimmed string / defined radio / `true` bare checkbox / non-empty array for a checkbox group) and `validateStep(step, answers)` returns `{ id, messageKey }` entries over `visibleFields` (so hidden conditionals like `como_chegou_outro` are skipped): required fields left empty → `validation.required`, then filled fields with a bad format → a format key. Format check so far: `email` type vs a pragmatic regex (`validation.email`); the serverless function stays the real gate. `state.errors` is a `Map` of field id → message key — populated on a blocked `goNext`, cleared per field by the answer setters as the user edits (text: error node removed in place, no re-render; radio/checkbox: re-render omits it), and reset wholesale on `goNext`/`goBack`. `renderStep` appends `<span class="error" data-for="<id>" role="alert">` with `t(messageKey)` and sets `aria-invalid` on the control. Keys `validation.required` / `validation.email` / `validation.phone` added to `locales/pt-PT.json` (full string pass is issue #6). Tests extended in `test/formEngine.test.mjs`.
  - **Phone country code**: `tel` fields render a country `<select>` (EU/EEA, Portugal default) plus a number input. Both feed one answer — a combined `"+351 912345678"` string — so no schema/field-id/spreadsheet change. List and `parsePhone`/`combinePhone` helpers live in `js/countries.js` (names inline Portuguese for now; `country.<code>` locale keys deferred to #6). `validateStep` checks the local part has 6–15 digits (`validation.phone`) once filled.
- **Issue #7 — Page 2a UI (`treino_geral`)**: no new markup — the schema-driven engine already renders this step. Landed Filipa's approved copy (informal "tu" register) into `locales/pt-PT.json` for the five fields + options (`onde_treina` "Casa"/"Ginásio", `frequencia_treino` "2-3x/semana"…). **Renamed four field ids** per the issue's table — `maior_dificuldade`→`dificuldade_atual`, `rotina_treinos`→`frequencia_treino`, `orientacao_nutricionista`→`orientacao_nutricional`, `compromisso_online`→`comprometimento` — a deliberate one-off exception to the "ids are stable" rule (user-approved; step never shipped, no payload/sheet impact yet). Option ids kept (`2_3x`/`4_5x`/`5_mais`, `casa`/`ginasio`). Locale key namespace followed: `form.field.*` / `form.option.frequencia_treino.*` (not `form.step2a.*`). Updated `docs/field-ids.md`, README "2a" row, and both test files (schema: field-id order + locale-key resolution for the step; engine: renders all 5 controls, validation gives 5 `validation.required` when empty / none when filled). Page 1 stays formal register — cross-step tone reconciliation is out of scope.
- **Issue #10 — Page 3b UI (`posparto`)**: applied the issue's approved copy to the 8 posparto fields; **renamed** `acompanhamento_exercicio_gravidez`→`acomp_exercicio_gravidez` and `fisio_pelvica_gravidez`→`acomp_fisio_gravidez` (one-off, step unshipped, per the #7 precedent). Replaced the step's `aviso_contacto` checkbox with the shared read-only `nota_contacto` note (`form.note.contacto_treinadora`, added in #9) — `aviso_contacto` is now unused and its locale key was removed (the bare-checkbox `renderField` path stays, now covered by a `renderField` unit test instead of a schema-driven one). Shared keys tweaked to the #10 copy: `form.field.preferencia_local` → "Preferência de treino presencial", `form.field.disponibilidade_horario` → "Qual a disponibilidade para treino" (affects the `gestacao` step too). Updated `docs/field-ids.md`, README 3a/3b rows, and both test files (schema: posparto field-id order + note-is-read-only + labelKey/textKey resolution; engine: renders 8 controls + `p.note`, 8 `validation.required` when empty / none when filled). This completes the gestação/pós-parto branch (#8–#10).
- **Issue #9 — Page 3a UI (`gestacao`)**: added a **`note` field type** — read-only informational text, no `labelKey`/`required`, carries a `textKey`. `renderField` emits `<p class="note">`; `validateStep` skips it (`field.type === "note"` → continue); never stored in `answers`. Used for the step's closing note (`form.note.contacto_treinadora`, shared key for #10 reuse), which **replaces the `aviso_contacto` checkbox** on this step (posparto keeps its checkbox until #10). **Renamed** `preferencia_treino_presencial` → `preferencia_local` (id + `form.field.*` + `form.option.preferencia_local.*`) across both `gestacao` and `posparto` — deliberate one-off (steps unshipped, per the #7 precedent); option ids `crossfit_4475`/`templo_fitness` kept. `css/main.css` gains a `.note` block. Updated `docs/field-ids.md` (new `note` row in the value-shape table, both step tables, shared option set), README 3a/3b rows, and both test files (schema: gestacao field-id order + note-is-read-only + labelKey/textKey resolution + generic loop now allows `note`; engine: renders 5 controls + `p.note` with no `nota_contacto` control, 5 `validation.required` when empty / none when filled).
- **Issue #8 — Page 2b UI (`fase_gestacao`)**: no new markup/engine/validation work — the schema-driven engine (#3–#5) already renders the single `fase` radio, routes to `gestacao`/`posparto` off `answers.fase`, and enforces `required`. Landed the issue's approved copy (informal "tu" register, matching the gestação branch): `form.field.fase` and `form.step.fase_gestacao.title` → "Em que fase te encontras"; options "Gestação"/"Pós-parto" already matched. Kept the existing `form.field.fase` / `form.option.fase.*` keys — did **not** add the `form.step2b.*` namespace the issue text suggested (repo convention from #6/#7). Added explicit Page-2b tests: engine renders the 2-option radio with resolved labels + 1/0 `validation.required`; schema test asserts every `fase_gestacao` labelKey resolves in `pt-PT.json`. Fixed the README "2b" row ("checkbox" → "radio").
- **Issue #11 — Confirmation and error screens**: the final visible step now ends with a submit button (`form.nav.submit` → "Enviar") instead of no forward control; `goSubmit` validates the last step (same path as `goNext`) then calls `runSubmit`. `js/submit.js` gained a mock `submitForm(payload)` — resolves `{ ok: true }` on a 0ms timer, throws when `globalThis.__MOCK_SUBMIT_FAIL` is set (test/manual hook); the real `fetch` POST to the Netlify function is deferred to the backend issue. `renderConfirmation(status)` replaces `#form-root` with a terminal screen: `<p class="confirmation">` holding `form.confirmation.success` (its `\n` — the copy's `<br>` — split into real `<br>` nodes via the DOM, no `innerHTML`) or `form.confirmation.error` plus a retry button (`form.confirmation.retry_button`). `state.submitting` guards against double submission: `runSubmit` early-returns while a request is in flight and re-renders the originating screen (step or error) with its button `disabled`; the flag clears before the terminal screen renders so retry stays clickable. `css/main.css` gains `.confirmation` + `.nav button[disabled]`. Tests added to `test/formEngine.test.mjs` (submit button on last step, mocked success → success screen, mocked failure → error screen + working retry, button disabled in flight / second trigger is a no-op, success copy line-break split).
- **Issue #14 — Google Sheets integration**: submission is now real for the Sheets side (email deferred to a later issue). `js/submit.js` `submitForm` POSTs the flat `answers` object to `/.netlify/functions/submit` and resolves the JSON body (`{ ok, flow }`); non-2xx rejects → existing error/retry screen. The `__MOCK_SUBMIT_FAIL` hook is kept (short-circuits before `fetch`); `test/formEngine.test.mjs`'s fetch shim gained a branch that answers the submit endpoint. New serverless code (all ESM `.mjs`; `package.json` gained `"type": "module"`): `netlify/functions/submit.mjs` exports `createHandler({ appendRow })` + a wired `handler` — guards non-POST (405), bad JSON (400 `bad_request`), missing `nome`/`email`/`contacto_telefonico` (400 `validation` + `fields`), else derives `flow` via `flowFor`, injects `submitted_at` (ISO), appends the row; a `stage: "sheets"` error → 502 `sheets_write_failed` (distinct `console.error` prefix), anything else → 500 `internal`. `netlify/functions/lib/columns.mjs` derives per-flow column order from `formSchema.js` (`note` fields excluded, `submitted_at` prepended, gestação+pós-parto ids deduped) and exports `flowFor`/`columnsFor`/`tabFor`/`serializeCell` (arrays→`", "`, bool→`Sim`/`Não`, nullish→`""`)/`rowFor`. `netlify/functions/lib/sheets.mjs` — `google-auth-library` JWT (`GOOGLE_SHEETS_PRIVATE_KEY` `\n`-unescaped) + Sheets v4 REST via global `fetch`; `appendRow({tab,headers,row}, deps)` probes `{tab}!1:1`, writes headers if empty, then `append`s; deps injectable for tests. Tabs: **`Geral`** / **`Gestação-Pós-parto`**. `netlify.toml` gained `[functions] node_bundler = "esbuild"` + `NODE_VERSION = "22"`; added `.nvmrc` (`22` — jsdom's transitive `@exodus/bytes` needs `require(ESM)`, which is Node 20.19+; Node 18 is also EOL). `package.json` `engines.node` `>=20.19`. New GitHub Actions: `.github/workflows/ci.yml` (test on PR / non-main push) + `deploy.yml` (test then `netlify-cli deploy --prod` on `main`; needs `NETLIFY_AUTH_TOKEN` + `NETLIFY_SITE_ID` secrets). New tests: `test/columns.test.mjs`, `test/sheets.test.mjs`, `test/submit.function.test.mjs`, `test/submit.test.mjs`. Docs: `docs/field-ids.md` "Spreadsheet tabs" section; README "Submission flow" + "Google Sheets setup" + "CI/CD".
- **Issue #15 — Email integration**: after the Sheets write, `submit.mjs` sends one best-effort notification email per submission via **Resend** (`POST https://api.resend.com/emails`, Bearer `EMAIL_API_KEY`). `createHandler` now takes a second injectable dep `sendNotification` (defaults to a no-op for partial-DI tests; wired `handler` binds the real impl); the call sits in its own `try/catch` that only `console.error`s — a failure (or missing `EMAIL_*` config) never changes the `200 { ok, flow }`. New `netlify/functions/lib/email.mjs`: `defaultDeps()` (`{ apiKey, to, from, fetchImpl }` from `EMAIL_API_KEY`/`COMPANY_EMAIL_TO`/`EMAIL_FROM`), `renderEmail({ flow, answers })` → `{ subject, html, text }` (placeholder subject `Nova inscrição — Geral|Gestação/Pós-parto`, HTML table + plain-text list, values HTML-escaped), `sendNotification(args, deps)` — returns silently when any of the three env vars is unset, throws an `err.stage = "email"` error (distinct from `"sheets"`) on a non-2xx. New `netlify/functions/lib/labels.mjs`: server-side label resolution derived from `formSchema.js` + `locales/pt-PT.json` (imported via `with { type: "json" }`) — `resolveField(id)`, `resolveValue(fieldId, value)` (option-id → label, array → joined, bool → `Sim`/`Não`, free-text passthrough), `notePairs(flow, answers)` → ordered `{ label, value }[]` over `columnsFor(flow)` minus `submitted_at`, skipping empty fields. `.env.example` + README gain `EMAIL_FROM`; README gains an "Email setup" section and a filled email step in the submission flow. New tests: `test/labels.test.mjs` (11), `test/email.test.mjs` (6), `test/submit.function.test.mjs` +3 (email sent after write / email throw still 200 / no email on Sheets failure). `npm test` → 113 passing.
- **Issue #6 — Page 1 UI (`dados_basicos`)**: added the Filipa-approved options for `como_chegou` (`redes_sociais`, `fisioterapia`, `amigos_familiares`) and `objetivo_treino` (`recomposicao`, `saude_longevidade`, `reforco_modalidade`, `forca_atletismo`) — **merged** alongside the pre-existing placeholder options, not replacing them (7 and 9 options now; `outro` / `gestacao_posparto` stay last, the latter still drives the branch). `ganho_massa` relabelled to "Aumento de massa muscular". Strings live under the existing `form.option.*` keys (not `form.step1.*`). `js/formEngine.js` gains `pruneHiddenFieldAnswers()` — after a radio/checkbox change it deletes stored answers (and errors) for current-step fields whose `condition` no longer passes, so unchecking "outro" clears `como_chegou_outro`. `css/main.css` filled with a clean minimal baseline (single column, `#form-root` max-width, styled inputs/fieldsets/`.tel-group`/`.error`/`.nav` buttons, `:focus-visible` ring, red `[aria-invalid]` border). `docs/field-ids.md` and both test files updated for the new option ids.
- **Issue #13 — Serverless function scaffold and environment config**: no new code — the scaffold #13 asked for (POST `submit` handler + payload validation, `netlify.toml` functions dir, env vars in `.env.example`) was already delivered ahead of it by **#14**, which then went further and replaced the mocked success with the real Google Sheets write. Closed as superseded. Intentional deviations from #13's literal text, all from #14: the handler is `netlify/functions/submit.mjs` (ESM), not `submit.js`; there is no `flow` field in the payload and no "`flow` is one of the expected values" check — the function derives `flow` from `answers` via `flowFor`; a valid POST does a real Sheets write, so with no `GOOGLE_SHEETS_*` creds it returns 502 `sheets_write_failed` rather than a mocked 200 (a 200 `{ ok, flow }` once creds are set). Local verification (handler invoked directly with fake `event`s): `GET` → 405 `method_not_allowed`; `POST` malformed JSON → 400 `bad_request`; `POST {}` → 400 `validation` + `fields: [nome,email,contacto_telefonico]`; `POST` valid identity → reaches the Sheets write (502 locally, no creds). `npm test` → 93 passing, unchanged.
- **Issue #16 — Client-side submission wiring**: largely delivered ahead of it by #11 (confirmation/error screens), #14 (real Sheets POST in `js/submit.js` → `/.netlify/functions/submit`), and #15 (email) — same superseded pattern as #13. `js/submit.js` already POSTs the flat `answers` object and routes success/error off the real response; `runSubmit` in `js/formEngine.js` already guards double-submit via `state.submitting`. Deliberately **not** adding client-side `timestamp`/`flow` enrichment the issue's literal text suggests — the function derives `flow` (`flowFor`) and injects `submitted_at` itself; `test/submit.test.mjs` asserts the POST body is the verbatim answers object. This issue added: (1) a visible loading state — new locale key `form.nav.submitting` ("A enviar…"), the submit/retry button relabels + `#form-root` gets `aria-busy="true"` while in flight (`runSubmit`, `renderStep`, `renderConfirmation`); (2) removed the `globalThis.__MOCK_SUBMIT_FAIL` dev escape hatch from `js/submit.js` — tests now drive the failure path with a real fetch stub (`test/submit.test.mjs`) / a `failSubmit` flag in the `test/formEngine.test.mjs` fetch shim. README "Submission flow" gained an issue-#16 note. `npm test` → 113 passing.
- **Issue #17 — End-to-end QA across both branches**: no product-code changes — a QA pass. Added `docs/qa-checklist.md`: a manual script covering all three branch paths, back/forward + mid-flow branch switching, per-step required-field validation, console cleanliness, the confirmation/error screens, a real-device (iOS/Android) section, and a Filipa sign-off line. Closed automated-coverage gaps: `test/formEngine.test.mjs` +9 (button-by-button walk of the geral / gestação / pós-parto branches ending in a real `goSubmit()`; empty required field mid-branch blocks; goal-unchecked mid-flow reroutes to geral — and `objetivo_treino` being emptied is itself a blocking required error until a replacement goal is picked; `fase` flip swaps the leaf step; `Voltar` skips validation; `como_chegou_outro` required once "outro" checked). `test/submit.function.test.mjs` +3 (`fase=gestacao` payload → row asserted cell-for-cell against the 19-col Gestação-Pós-parto layout, pós-parto cells blank; submission still 200 with no `sendNotification` dep). `test/columns.test.mjs` +1 (geral projection ignores stale `fase`/leaf answers left after a branch switch). `npm test` → 124 passing. **Live run** (handler invoked directly with real `.env` — real Sheets + Resend): geral / gestação / pós-parto all → `200 { ok, flow }`; read-back confirmed each tab's header row equals `columnsFor(flow)` and the QA rows carry correct values in column order (Sheets trims trailing empty cells on append, so a gestação row is physically shorter than 19 cells — read by header index, not length). Confirmed behaviour: email failure never fails the submission (own `try/catch`, `console.error` only, still `200`); Sheets failure always fails it (`stage:"sheets"` → 502, else → 500 — either way non-2xx → client error/retry screen). Radios are stored in the sheet as raw option ids (`sim`, `casa`, `crossfit_4475`); label resolution happens only in the notification email (`lib/labels.mjs`). QA rows (`nome` starts `QA TEST`) left in the live sheet unless the user asks to delete them; real-device pass and Filipa sign-off remain a manual hand-off.
- **Phone country picker with SVG flags**: the `tel` field's native country `<select>` is replaced by a hand-rolled ARIA listbox (`buildCountrySelect` in `js/formEngine.js`) — a `type="button"` toggle showing the selected flag `<img>` + dial code, and a `[role="listbox"]` of `[role="option"]` rows (flag + `"<name> (<dial>)"`). Keyboard: ↑/↓/Home/End move, Enter/Space pick, Esc closes, a–z typeahead by country name; outside-click closes (listener added on open, removed on close). Flag SVGs vendored from `flag-icons` (MIT) into `img/flags/<code>.svg`, referenced as `img/flags/${c.code.toLowerCase()}.svg` with `alt=""` (decorative); `img/flags/README.md` credits the source. Native `<select>` couldn't render flag images and emoji flags fall back to letter pairs on Windows — hence the custom widget. Stored answer (`"+351 912345678"`), field id, schema, payload, sheet columns, locales all unchanged; `parsePhone`/`combinePhone` untouched. `js/countries.js` `flagEmoji` (added then) removed as dead. `css/main.css`: `.country-select*` + `.flag` blocks replace the old `.tel-group select` rule. Tests: `test/formEngine.test.mjs` tel-render/pick tests rewritten for the widget; `flagEmoji` test dropped. `npm test` → 126 passing (1 pre-existing unrelated failure in `test/labels.test.mjs` from an uncommitted `locales/pt-PT.json` label edit).
- **Spec recap — Page 1 options trimmed + gestação/pós-parto labels aligned**: a full recap against Filipa's approved field list found Page 1 still carried the issue-#6 placeholder options *merged* alongside the approved ones. Removed the non-spec option ids from `js/formSchema.js` and their `locales/pt-PT.json` keys: `como_chegou` → `instagram`/`recomendacao`/`pesquisa` gone (now 4 options: `redes_sociais`, `fisioterapia`, `amigos_familiares`, `outro`); `objetivo_treino` → `perda_peso`/`condicao_fisica`/`saude_bem_estar` gone (now 6: `ganho_massa`, `recomposicao`, `saude_longevidade`, `reforco_modalidade`, `forca_atletismo`, `gestacao_posparto`). Reverses the #6 "merged alongside" decision. Also rewrote 5 gestação/pós-parto field labels to the exact approved wording (`fisio_pelvica`, `semanas_gravidez`, `historial_risco`, `disponibilidade_horario` — shared by both leaf steps —, `complicacoes_parto`); `disponibilidade_horario` and `complicacoes_parto` now carry the parenthetical instructions. Single-select fields stay `radio` (spec's "checkbox" = generic tick-box). No field ids changed → payload/sheet columns unchanged. Fixtures using the removed option ids swapped to surviving ids across `test/columns.test.mjs`, `test/submit.function.test.mjs`, `test/labels.test.mjs`, `test/email.test.mjs`; `test/formEngine.test.mjs` count test now expects 4/6; `test/email.test.mjs` + `test/labels.test.mjs` label assertions updated to the new `semanas_gravidez` wording. `docs/field-ids.md` option lists updated.
- **Mobile-first CSS pass**: `css/main.css` restructured so base rules target a 360px phone; scale-up via `@media (min-width: 600px)` (type + control sizing) and `@media (min-width: 900px)` (page gutters, logo, header). The old `@media (max-width: 520px)` down-query is gone (folded into base); the `min-width: 760px` query became `900px`. Mobile hygiene added: all inputs/controls `font-size: 16px` on mobile (stops iOS focus-zoom), `100dvh` on html/body, `env(safe-area-inset-*)` gutters on `.page`, `-webkit-tap-highlight-color: transparent` on interactive elements, country-list options `padding: 12px 14px` (~48px touch targets), country `__list` `width: max-content` + `max-width: calc(100vw - 32px)` (no viewport overflow at 360px, `min-width: 260px` restored ≥600px), `.tel-group input` `min-width: 0` so it shrinks in the flex row. Also fixed: the reskin's `label.field > span:first-child { display:block }` was overriding `.tel-group`'s flex row — scoped out via `:not(.tel-group)`. No JS/markup change; `css`-only. `npm test` unaffected.
- **Issue #44 — Page 2a heading copy**: `form.step.treino_geral.title` in `locales/pt-PT.json` changed `"Treino geral"` → `"SOBRE O TREINO"` (client feedback; revises #7). Heading was never hardcoded — rendered via `t()` from `titleKey` in `js/formSchema.js`; no all-caps "TREINO GERAL" ever existed. Field/option ids, payload, sheet columns unchanged. Updated the 5 `title()` assertions in `test/formEngine.test.mjs`, plus doc heading references in `README.md` (flow diagram + page table) and `docs/qa-checklist.md`. `npm test` → 131 passing.
- **Issue #46 — Submission success message**: `form.confirmation.success` in `locales/pt-PT.json` changed to `"Em breve serás contactado via WhatsApp pela nossa equipa. Obrigado!"` (client feedback; revises #11). New copy is a single line — the old `\n` before "Obrigado!" is gone, so `renderConfirmation()` (unchanged; still splits on `\n`) now emits zero `<br>`. Updated `test/formEngine.test.mjs` (3 `startsWith` assertions on the old copy; the "splits the copy on its line break" test renamed + now asserts 0 `<br>`) and the success-screen line in `docs/qa-checklist.md`. No JS/schema/payload change.
- **Issue #45 — move `comprometimento` to its own page**: split the `comprometimento` radio out of `treino_geral` into a new standalone step `id: "comprometimento"` in `js/formSchema.js`, placed immediately after `treino_geral` with the same `condition` (`!selectedGestacaoPosparto`). Field id/type/options unchanged (`comprometimento` radio, `sim`/`nao`). New locale key `form.step.comprometimento.title` → `"COMPROMISSO"` (only new string; issue said none needed but a step needs a `titleKey` for `renderStep`'s `t(step.titleKey)`). `formEngine.js` needed **no** nav change — index/back/progress all derive from `visibleSteps(answers)`, so the general branch is now 3 steps (`dados_basicos` → `treino_geral` → `comprometimento`) and the progress indicator counts it automatically. `netlify/functions/lib/columns.mjs`: `GERAL_COLUMNS` now appends `...stepFieldIds("comprometimento")` (it enumerates steps explicitly), keeping the `comprometimento` column last in the Geral tab — payload/sheet layout unchanged. Gestação/pós-parto branch untouched. Tests updated: `test/formSchema.test.mjs` (step id list + geral `visibleStepIds` now include `comprometimento`; `treino_geral` field list drops it; new "own step" test), `test/formEngine.test.mjs` (forward-nav chains through `comprometimento`; `treino_geral` now 4 fields / 4 required errors; new `comprometimento`-step render+validate test; progress "Passo 1 de 3" / 33%; `onFilledLastStep` + submit/blocked tests retarget `currentStepId` to `comprometimento`; geral E2E walk gains a `clickAdvance()` to the new page). `npm test` → 133 passing. Docs: `docs/field-ids.md` (new step section + Geral columns line), `docs/qa-checklist.md` (Branch 1 flow, step 2/3 checks, validation list), `README.md` (flow diagram gains the `Compromisso` node, page table row).
- **Issue #47 — Landing page before the form**: neutral intro screen shown before Página 1. New `js/landing.js` — `renderLanding(onStart)` paints `#form-root` (DOM-only, no `innerHTML`) with a centered logotype, tagline, message, and a "Começar" CTA. Lives **outside** `formSchema.js` (collects no answers). `js/formEngine.js`: `init()` now hides `.page-header` and calls `renderLanding(startForm)` instead of `renderStep()`; new exported `startForm()` reveals the header and renders step 1 on CTA click (no reload). `index.html` `<header class="page-header">` gains `hidden` (no logo flash pre-`init()`). Copy in `locales/pt-PT.json` under `landing.message` / `landing.tagline` / `landing.cta` (verbatim from issue). `css/main.css` `.landing*` block uses existing tokens as **placeholders** — client's final logotype + palette/fonts still pending; `img/ignite.png` is a stand-in, swap point commented. Tests: `test/formEngine.test.mjs` +2 (init shows landing not form; CTA starts form + reveals header) — the shared `before(init)` now resets `document.body` past the landing so the rest of the suite drives the form directly. `npm test` → 135 passing. Docs: README (form-flow note + `js/landing.js` in structure), `docs/qa-checklist.md` (landing-screen section).
- **Palette lock-in + logo swap + typeface comparison tool**: follow-up to #48, replacing its placeholder hues with client-approved hex and adding a fixed light theme for the pre-answer screens. `js/theme.js`: `DEFAULT_THEME.bg`/`bgAccent` → `#899064` (was `#000`); `THEME_BY_OBJETIVO.gestacao_posparto.bg`/`bgAccent` → `#CA9A8E` (was placeholder plum); new `NEUTRAL_THEME` (`bg: #F2F0EB`, `text: #1a1a1a`) for landing + `dados_basicos`, applied via new `applyNeutralTheme()` instead of the old `applyTheme({})` sentinel-object trick; every theme gained `confirmationBg` (`#899064` default / `#EAA794` gestação — the confirmation screen no longer reuses the preceding step's `bg`) and `logo` (`"green"` / `"pink"`); `applyTheme(answers, root, { confirmation })` now also writes `--text` (was previously untouched, relying on a single global value — no longer safe once one theme needs dark text and the rest need light) and takes a `confirmation` option to swap in `confirmationBg`. New `logoForAnswers(answers)` resolves the logo variant. `js/formEngine.js` gained a small `setLogo(logo)` helper (sets `#header-logo` and `.landing-logo` `src` to `img/ignite-<green|pink>.png`) called from `renderStep`/`renderConfirmation`/`init` alongside the theme calls. **Placeholder logo assets**: `img/ignite-green.png` and `img/ignite-pink.png` are currently copies of the original `img/ignite.png` — the client's real transparent green/pink logo files still need to be dropped in (same filenames) to replace them. New **`js/typeface.js`**: a manual, non-production comparison tool — `?typeface=playfair` / `?typeface=lora` in the URL (persisted to `localStorage`) overrides `--font-display` for a live Playfair Display vs. Lora vs. current-default (Bebas Neue) comparison; not a real visitor-facing A/B test, and the shipped default is untouched until the team picks a winner. `index.html` gained the Playfair Display + Lora Google Fonts `<link>`. Tests: `test/theme.test.mjs` updated for the new hex values + `confirmationBg`/`logo`/`NEUTRAL_THEME`/`applyNeutralTheme`/`logoForAnswers`; `test/formEngine.test.mjs` updated (`dados_basicos` now asserts against `NEUTRAL_THEME` instead of `DEFAULT_THEME`) plus new tests for confirmation-screen theming and logo swapping. `npm test` → 174 passing. Docs: README theming paragraph rewritten, `docs/qa-checklist.md` theming/logo/typography-toggle checks updated. Branch: `feature/palette-logo-typography`.
- **Issue #48 — Dynamic page theming based on selected objetivo**: the page takes a distinct palette (background + accent) **only when `objetivo_treino` includes `gestacao_posparto`** — every other selection (single or multi) keeps the default — and **only on the steps after `dados_basicos`** + the confirmation screen (landing + `dados_basicos` always neutral: green / black). New `js/theme.js` — the single colour config: `DEFAULT_THEME` (`bg` `#000` + green brand) + `THEME_BY_OBJETIVO` (currently one **placeholder** `gestacao_posparto` entry — `bg` + `accent*` —, swap point for the client's finals; add more option-id keys to theme other objectives), pure `themeForAnswers(answers)` (first selected `objetivo_treino` id that has a mapping, else default), and `applyTheme(answers, root = document.documentElement)` which writes `--bg` / `--accent` / `--accent-bright` / `--accent-hover` / `--accent-soft` as CSS custom properties (full set every time so deselect reverts). `css/main.css`: `:root` gains the `--accent*` tokens seeded to the greens; every **named** `var(--green*)` reference repointed to `var(--accent*)`, and the flat `rgba(74,120,58,…)` fills (option hover/checked bg, `.note` bg, country-option highlight) too — raw shadow/glow `rgba` literals and `::selection` left as-is (documented limitation; full glow theming deferred to real palettes); `body` background is a `linear-gradient(180deg, #000 0, var(--bg-accent) 360px)` — black under the always-black `.page-header` (logo PNG is on solid black), easing into the themed lower stop; themes carry `bgAccent` (`applyTheme` writes `--bg-accent`), default `#000` so the default page is flat black. `body` keeps a `background-color` transition (disabled under `prefers-reduced-motion`). `js/formEngine.js`: `renderStep()` calls `applyTheme(state.currentStepId === "dados_basicos" ? {} : state.answers)`, `renderConfirmation()` calls `applyTheme(state.answers)`, `init()` calls `applyTheme({})`; no nav change — a checkbox toggle already re-renders the step, so select/deselect/back all re-theme for free. Tests: new `test/theme.test.mjs` (4); `test/formEngine.test.mjs` +4 (themed step gets gestação accent+bg; `dados_basicos` stays default with `gestacao_posparto` picked; other-objectives step stays default; clearing reverts). `npm test` → 143 passing. Docs: README (structure + form-flow paragraph), `docs/qa-checklist.md` (theming checks in Branch 1). **Still pending from client**: final palette/fonts for the gestação theme.
